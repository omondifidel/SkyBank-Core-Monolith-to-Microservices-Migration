server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Professional touch: A proxy to avoid CORS issues
    location /api/v1/ {
        proxy_pass http://backend:8000/api/v1/;
    }
}